# Sprint 7: Demo 场景验证 + 优化

> 目标：用两个内置 Demo 场景完整跑通全流程，发现并修复体验问题，打磨 UI/UX，
> 让 AI Journey Copilot MVP 达到可演示状态。

## Demo 场景

### 场景 1：Amy 机场接机（目标驱动）
- 用户画像：Amy（女性，28 岁，市场经理，经常出差）
- 初始预设：`amy_airport_pickup`
- 流程："我要去浦东机场接人" → 候选选择 → 方案推荐 → 方案选择 → 行程就绪 → 确认出发 → 行程中 → 到达
- 验证点：路线 polyline、目的地 marker、ETA 浮动卡片、停车搜索、到达引导

### 场景 2：聚会找餐厅（意图驱动）
- 用户画像：Claire（女性，25 岁，设计师，美食爱好者）
- 初始预设：聚会场景
- 流程："我想找个附近的日料店和朋友聚餐" → 澄清追问 → POI 候选 → 方案推荐 → 选择 → 出发
- 验证点：澄清卡片、POI 候选选择、3 套方案对比

## Phase 1：Sprint 6 收尾 + 基线验证
- [x] 地图 polyline 渲染（修复了 AMap 编码 → 明文解码）
- [x] 目的地 Marker 渲染
- [x] 停车场 Marker 渲染（到达阶段）
- [x] 右上角 ETA 浮动卡片
- [x] 消息卡片合并渲染
- [x] TypeScript 编译零错误
- [ ] Vite 生产构建通过（待验证）

## Phase 2：后端数据契约核查
- [x] 路线 polyline 格式统一（后端解码为 "lon,lat;lon,lat" 明文）
- [x] 停车场坐标字段统一（location: {lat, lon}）
- [x] ready 卡片包含完整路线/ETA 数据
- [x] arriving 卡片包含停车数据 + 目的地坐标
- [x] 重规划结果字段归一化（new_* → 标准字段名）

## Phase 3：场景 1 — 机场接机端到端验证
- [ ] 冷启动加载 Amy 预设 + GCP 数据显示
- [ ] 输入"去浦东机场接人" → 意图识别正确
- [ ] 候选 POI 列表显示（浦东机场 T1/T2 等）
- [ ] 选择候选 → 直接写入目的地，不重新解析
- [ ] 3 套路线方案推荐卡片显示
- [ ] 选择方案 → 进入规划状态
- [ ] JourneyReady 卡片显示（路线概要 + ETA + 距离）
- [ ] 地图上显示路线 polyline + 目的地 marker
- [ ] 右上角浮动 ETA 卡片显示
- [ ] 点击"确认出发" → 进入 in_progress 状态
- [ ] 顶部状态横幅更新为"行程中"
- [ ] ETA 定时更新（GCP 后台监听）
- [ ] 到达阶段 → 停车搜索 → ParkingCard + 停车场 marker
- [ ] 到达完成 → 行程总结

## Phase 4：场景 2 — 意图驱动验证
- [ ] 切换到 Claire 画像 + 聚会场景
- [ ] 输入"找个附近的日料店" → 进入澄清
- [ ] ClarifyCard 显示追问 + 候选选项
- [ ] 回复澄清 → 进入推荐
- [ ] 3 套方案（不同餐厅 + 路线）
- [ ] 选择方案 → 规划 → 出发
- [ ] 路线 + ETA + 地图 marker 正确显示

## Phase 5：UX 优化
- [x] 空状态/欢迎页 + 快捷建议按钮
- [x] 卡片入场动画（淡入 + 上滑 + 高亮脉冲）
- [x] 消息入场动画
- [ ] 初始加载动画 / 骨架屏
- [ ] 状态切换过渡动画
- [ ] 错误重试机制
- [ ] 对话历史样式优化
- [ ] 移动端适配检查
- [ ] 深色主题细节打磨（阴影、间距、圆角）
- [x] 滚动条美化

## Phase 6：性能 & 稳定性
- [ ] WebSocket 断线重连
- [ ] 长对话内存泄漏检查
- [ ] 卡片数量上限 / 懒渲染
- [ ] 错误边界（React Error Boundary）

## Phase 7：收尾
- [ ] SPRINT_PROGRESS.md 更新（标记 Sprint 6 完成 + Sprint 7 进度）
- [ ] README.md 更新
- [ ] Demo 使用说明
